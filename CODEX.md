# Codex

**Read `AGENTS.md` first.** It is the contract for every assistant on this
repository — Codex, Claude, Antigravity, or any other — and it holds the hard
rules, where the current state lives, and the checks before calling a slice
complete.

Nothing is repeated here on purpose. Two copies of a rule drift apart, and in
this repository that has already happened.
