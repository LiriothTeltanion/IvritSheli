# Repintado Nocturne — Candidate scene review pack

This folder stores the 26 scene images from:

- `C:\Users\kevin\Downloads\Repintado Nocturne_files\saved_resource.html`

They are imported as **reference review material only**. Use them to compare:

- anatomy and pose consistency,
- material realism,
- adult tone and scene context,
- whether the anchor remains identifiable at small scales.

`manifest.json` maps every file with:

- `alt` (semantic key + state tag),
- `title` (scene key),
- `note` (auto-collected review note from the artifact),
- `file`, `mime`, `size`.

These files are not shipped to learners and do not replace the canonical SVG
scene implementation.
